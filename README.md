# SHARK — Telegram Downloader Bot & REST API

نسخه: 10.0  
نویسنده: K-z89  
مجوز: MIT

شرح کوتاه
---------
SHARK یک ربات تلگرام و REST API برای دانلود محتوا از پلتفرم‌های محبوب (مانند Instagram، YouTube، Twitter/X، TikTok، Facebook و Reddit) است. این پروژه برای استفاده در محیط‌های متن‌باز طراحی شده و قابلیت‌های تبدیل فرمت، پشتیبانی از کیفیت‌های HD/4K، کش هوشمند و پنل مدیریت را فراهم می‌کند.

ویژگی‌ها
--------
- دانلود از:
  - Instagram (پست، ریل، استوری)
  - YouTube (ویدیو، شورت، استخراج صدا)
  - X / Twitter
  - TikTok
  - Facebook
  - Reddit
- پشتیبانی کیفیت HD و 4K
- تبدیل فرمت به MP3 / MP4
- پنل ادمین پیشرفته (PHP)
- رابط کاربری Inline Keyboard در تلگرام
- دانلود همزمان چند فایل (concurrent downloads)
- سیستم کش هوشمند برای جلوگیری از دانلودهای تکراری
- گزارش‌گیری آماری
- پشتیبانی از Proxy و تنظیمات شبکه
- محدودیت حجم و Rate Limit قابل تنظیم

پیش‌نیازها
----------
- Python 3.8+
- PHP 7.4+ (برای پنل/نسخه‌های مرتبط با PHP)
- یک توکن ربات تلگرام (Telegram Bot Token)
- دسترسی به اینترنت
- (اختیاری) Proxy برای محیط‌هایی که دسترسی مستقیم ندارند

ساختار پروژه
-------------
SHARK/
├── bot.py              # ربات تلگرام (Python)
├── api.py              # سرور FastAPI (Python)
├── core.py             # موتور دانلود (yt-dlp / instaloader wrapper)
├── shark_api.php       # API / پنل PHP
├── shark_bot.php       # ربات PHP (در صورت استفاده)
├── admin/              # پنل مدیریت (PHP)
├── config.py           # تنظیمات اصلی
├── requirements.txt    # وابستگی‌های Python
└── README.md           # مستندات (این فایل)

نصب سریع (نسخه پایه)
--------------------
```bash
git clone https://github.com/K-z89/SHARK.git
cd SHARK

# نصب وابستگی‌ها (Python)
pip install -r requirements.txt

# اجرای ربات
python bot.py

# یا اجرای API
python api.py
```

متغیرهای محیطی (Environment Variables)
-------------------------------------
تنظیم این متغیرها برای راه‌اندازی مورد نیاز است:

```env
BOT_TOKEN=توکن_ربات
ADMIN_IDS=123456789,987654321    # لیست ID ادمین‌ها، جداشده با کاما
API_HOST=0.0.0.0
API_PORT=8000
MAX_FILE_SIZE=2147483648         # حداکثر حجم فایل بر حسب بایت (مثال: 2GB)
RATE_LIMIT_PER_MIN=100
CACHE_TTL_SECONDS=300
PROXY_URL=http://user:pass@proxy:port   # اختیاری
ENCRYPTION_KEY=base64_or_hex           # برای رمزنگاری اطلاعات حساس
```

اجرای محلی با Docker (پیشنهاد)
------------------------------
(نمونه Dockerfile / docker-compose را می‌توان اضافه کرد؛ در این نسخه پایه دستورالعمل دستی ارائه شده)

مثال کد — شروع ربات (نمونه Python)
---------------------------------
```python
import asyncio
from telegram import Update
from telegram.ext import Application, CommandHandler

class SharkBot:
    def __init__(self, token):
        self.token = token
        self.app = Application.builder().token(self.token).build()

    async def start(self, update: Update, context):
        await update.message.reply_text("🦈 SHARK Ready!")

    def run(self):
        self.app.add_handler(CommandHandler("start", self.start))
        self.app.run_polling()

if __name__ == "__main__":
    import os
    bot = SharkBot(os.environ.get("BOT_TOKEN"))
    bot.run()
```

نمونه درخواست API
------------------
- POST /download  
  توضیح: ارسال URL برای دانلود و دریافت لینک فایل یا اعمال تبدیل

مثال curl:
```bash
curl -X POST "http://localhost:8000/download" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://www.youtube.com/watch?v=...",
    "format": "mp4",
    "quality": "1080",
    "convert_to": "mp3"
  }'
```

- GET /stats  
  توضیح: نمایش آمار سیستم (فایل‌های دانلودشده، ترافیک، ...)
```bash
curl http://localhost:8000/stats
```

- GET /health  
  توضیح: وضعیت سلامت سرویس (uptime، متریک‌های پایه)
```bash
curl http://localhost:8000/health
```

دستورات ربات تلگرام
-------------------
- /start  — شروع و خوش‌آمدگویی  
- /help   — نمایش راهنما و نمونه‌ها  
- /stats  — نمایش آمار (فقط ادمین)  
- /admin  — ورود به پنل مدیریت (فقط ادمین)  
(روند دانلود مبتنی بر ارسال URL یا استفاده از دکمه‌های Inline برای انتخاب فرمت/کیفیت)

تنظیمات و پیکربندی
-------------------
- Rate limiting: محدودیت پیش‌فرض 100 درخواست در دقیقه (قابل تغییر از طریق ENV)  
- Cache: TTL پیش‌فرض 5 دقیقه برای نتایج دانلود (قابل تنظیم)  
- Storage: ذخیره‌سازی محلی و کش؛ برای مقیاس‌پذیری می‌توان S3 یا استوریج ابری اضافه کرد  
- Proxy: پشتیبانی از پروکسی برای تراکنش‌های شبکه‌ای

امنیت
-----
- Rate limit برای جلوگیری از سوءاستفاده  
- اعتبارسنجی ورودی‌ها (validate URL، اندازه‌ها و پارامترها)  
- رمزنگاری اطلاعات حساس (توکن‌ها / کلیدها) در استوریج/لاگ‌ها  
- محدودسازی قابلیت اجرای کد یا دانلود فایل‌های خارج از محدوده تعریف‌شده  
- احراز هویت پنل ادمین و حفاظت مسیرهای مدیریتی

محدودیت‌ها
----------
- حداکثر حجم فایل: پیش‌فرض 2GB (قابل تغییر)  
- Rate Limit: پیش‌فرض 100 درخواست در دقیقه  
- Cache TTL: پیش‌فرض 5 دقیقه  
- ممکن است برخی ویدیوهای محافظت‌شده یا DRM قابل دانلود نباشند  
- پشتیبانی از کیفیت 4K وابسته به منبع و ابزار yt-dlp

قابلیت‌های فنی و ابزارهای اصلی
------------------------------
- Backend: Python (FastAPI برای API)  
- Bot Framework: python-telegram-bot  
- دانلود و استخراج: yt-dlp، Instaloader (برای اینستاگرام) و کتابخانه‌های مرتبط  
- پنل مدیریت: PHP (shark_api.php و اجزای admin/)  
- ذخیره‌سازی: کش و استوریج محلی (قابل ارتقا به استوریج ابری)

چند نکته درباره توسعه
--------------------
- برای اضافه کردن پلتفرم جدید، موتور core.py را گسترش دهید تا رابطی واحد (single download interface) ارائه دهد.  
- برای مقیاس‌پذیری: اجرای سرویس API با Gunicorn/Uvicorn در پس‌زمینه، استفاده از queue (مثلاً Redis + RQ/Celery) برای پردازش دانلودهای سنگین و ذخیره‌سازی نتایج در S3.  
- برای تست: از تست‌های واحد برای لایه‌های استخراج URL، پردازش صف و مبدل‌های فرمت استفاده کنید.

مسائل حقوقی و اخلاقی
--------------------
- کاربر مسئول اطمینان از حقوق نشر و استفاده قانونی از محتوای دانلودشده است.  
- SHARK ابزارِ دانلود فراهم می‌کند؛ استفاده نادرست یا نقض حق نشر بر عهده کاربر است.

نسخه‌های آینده (Roadmap)
------------------------
- افزودن پلتفرم‌های بیشتر  
- رابط وب کاربری تعاملی (SPA)  
- اپلیکیشن موبایل  
- سیستم پرداخت برای خدمات پرمیوم  
- بهبود سیستم صف و مقیاس‌پذیری (microservices)

تماس و پشتیبانی
---------------
- توسعه‌دهنده: K-z89  

- مخزن: https://github.com/K-z89/Shark-

مشارکت
------
پذیرای مشارکت‌های متن‌باز هستیم. اگر می‌خواهید ویژگی جدید اضافه کنید یا باگ گزارش کنید:
1. Fork کنید
2. برنچ جدید بسازید
3. Pull Request ارسال کنید همراه با توضیح مفصل و testcase (در صورت امکان)

مجوز
----
این پروژه تحت مجوز MIT منتشر می‌شود. برای اطلاعات بیشتر فایل LICENSE را مشاهده کنید.

ضمیمه — نمونه فایل requirements.txt (نمونه)
--------------------------------------------
```text
fastapi
uvicorn[standard]
python-telegram-bot==20.0
yt-dlp
instaloader
requests
pydantic
python-dotenv
```

ضمیمه — نکات راه‌اندازی تولید (Production)
-----------------------------------------
- از سرویس‌های process manager مانند systemd یا supervisor برای نگهداری سرویس استفاده کنید.  
- از HTTPS (TLS) برای API و وب‌پنل استفاده نمایید.  
- کلیدهای حساس را در Secret Manager یا محیط امن ذخیره کنید.  
- مانیتورینگ و لاگینگ (Prometheus / Grafana / ELK) برای پایش سرویس پیشنهاد می‌شود.

پایان
-----
این README بر مبنای مشخصات ارائه‌شده تنظیم شده است. اگر بخواهید، من می‌توانم:
- همین README را به انگلیسی ترجمه و GitHub-ready انگلیسی تحویل دهم،  
- یا نسخه‌ای مخصوص جذب ستاره و مشارکت‌کننده (contributor-friendly README) آماده کنم.  
بگویید کدام را می‌خواهید تا بلافاصله آماده کنم.
