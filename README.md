# SHARK — ربات دانلودر تلگرام (نسخه 10.0)

نسخهٔ کوتاه: SHARK یک ربات تلگرام همراه با یک API RESTful برای دانلود محتوا از پلتفرم‌های محبوب (اینستاگرام، یوتیوب، X، TikTok و غیره) با پشتیبانی از تبدیل به MP3/MP4، کیفیت HD/4K و پنل مدیریت است.

نسخه: 10.0  
توسعه‌دهنده: K-z89  
زبان‌ها: Python (FastAPI, yt-dlp, Instaloader), PHP  
لایسنس: MIT

---

## فهرست مطالب
- معرفی
- قابلیت‌ها
- پیش‌نیازها
- ساختار پروژه
- تنظیمات (Environment)
- راه‌اندازی سریع
- نمونه استفاده (ربات و API)
- Endpoints API
- دستورات ربات
- ویژگی‌های فنی و امنیتی
- محدودیت‌ها
- توسعه و مشارکت
- تماس و پشتیبانی
- مجوز

---

## معرفی
SHARK یک راه‌حل کامل برای دانلود خودکار محتوا از شبکه‌های اجتماعی و سرویس‌های ویدیویی است. مناسب برای ساخت بات‌های شخصی یا سرویس‌هایی که نیاز به تبدیل و نگهداری موقت فایل‌ها دارند.

---

## قابلیت‌ها (Highlights)
- دانلود پست‌ها، ریلز و استوری‌های اینستاگرام (Instaloader)
- دانلود ویدیوها، شورت‌ها و استخراج صوت از یوتیوب (yt-dlp)
- دانلود از X/توییتر، TikTok، Reddit، Facebook و غیره
- کیفیت خروجی: HD و 4K (در صورت موجود بودن)
- تبدیل فرمت‌ها: MP4, MP3, JPG/PNG, GIF
- پشتیبانی از دانلودهای همزمان
- کش هوشمند برای جلوگیری از دانلود مجدد
- پنل مدیریت (PHP) با امکانات آماری
- پشتیبانی از Proxy، Rate Limit و Validation
- رابط کاربری Inline Keyboard در ربات تلگرام

---

## پیش‌نیازها
- Python 3.8+
- PHP 7.4+
- pip (برای نصب پکیج‌های پایتون)
- دسترسی اینترنت
- توکن ربات تلگرام

پکیج‌های پیشنهادی (نمونه در requirements.txt):
- fastapi
- uvicorn
- python-telegram-bot
- yt-dlp
- instaloader
- aiohttp
- python-multipart
- redis (اختیاری برای کش)

---

## ساختار پروژه
```
SHARK/
├── bot.py              # ربات تلگرام (Python)
├── api.py              # سرور FastAPI (Python)
├── core.py             # موتور دانلود (Python)
├── shark_api.php       # API / پنل مدیریت (PHP)
├── shark_bot.php       # نسخهٔ PHP ربات (اختیاری)
├── admin/              # پنل ادمین و صفحات مربوطه
├── config.py           # تنظیمات مرکزی
├── requirements.txt    # کتابخانه‌ها
└── README.md           # مستندات
```

---

## تنظیمات (Environment)
فایل نمونه `.env`:
```env
BOT_TOKEN=توکن_ربات
ADMIN_IDS=123456789,987654321
API_HOST=0.0.0.0
API_PORT=8000
MAX_FILE_SIZE=2147483648   # 2GB
RATE_LIMIT=100             # درخواست/دقیقه
CACHE_TTL=300              # ثانیه (5 دقیقه)
PROXY=http://user:pass@host:port  # اختیاری
```

مقادیر را مطابق نیاز خود تغییر دهید.

---

## راه‌اندازی سریع (Local)
1. کلون کردن مخزن:
```bash
git clone https://github.com/K-z89/SHARK.git
cd SHARK
```

2. ایجاد و فعال کردن محیط مجازی (پایتون):
```bash
python -m venv venv
source venv/bin/activate    # لینوکس / macOS
venv\Scripts\activate       # ویندوز
```

3. نصب وابستگی‌ها:
```bash
pip install -r requirements.txt
```

4. تنظیم متغیرهای محیطی در `.env`.

5. اجرای API:
```bash
uvicorn api:app --host 0.0.0.0 --port 8000
```

6. اجرای ربات:
```bash
python bot.py
```

در صورت استفاده از نسخهٔ PHP پنل یا ربات، آن را در وب‌سرور (مثل Apache/Nginx + PHP-FPM) مستقر کنید.

---

## نمونهٔ استفاده

- ارسال پیام به ربات:
  - کاربر URL یک پست/ویدیو را به ربات ارسال می‌کند
  - ربات لینک را تحلیل می‌کند و گزینه‌های کیفیت/فرمت را نشان می‌دهد
  - کاربر انتخاب می‌کند و فایل آماده و ارسال می‌شود

- نمونهٔ تابع شروع (Python):
```python
import asyncio
from telegram import Update
from telegram.ext import Application, CommandHandler

class SharkBot:
    def __init__(self, token):
        self.token = token
    
    async def start(self, update: Update, context):
        await update.message.reply_text("🦈 SHARK Ready!")
```

- مثال curl برای دانلود از API:
```bash
curl -X POST "http://localhost:8000/download" \
  -H "Content-Type: application/json" \
  -d '{"url":"https://www.youtube.com/watch?v=...","format":"mp4","quality":"1080p"}'
```

---

## API Endpoints (نمونه)
- POST /download  
  پارامترها: `url` (رکویست)، `format` (mp4/mp3)، `quality`، `notify`  
  پاسخ: Job ID یا لینک دانلود پس از آماده‌سازی

- GET /stats  
  بازگشت آمار کلی (تعداد درخواست‌ها، حجم دانلود شده، کش‌ها)

- GET /health  
  وضعیت سرویس (uptime, dependencies)

مثال پاسخ (JSON) برای /download:
```json
{
  "job_id": "abc123",
  "status": "queued",
  "estimated_time": 12
}
```

---

## دستورات ربات
```
/start - شروع و خوش‌آمدگویی
/help  - راهنمای استفاده
/stats - نمایش آمار (برای ادمین‌ها محدود شده)
 /admin - دسترسی به پنل مدیریت (برای ادمین‌ها)
```

در ربات از Inline Keyboard برای انتخاب کیفیت/فرمت و مشاهده وضعیت دانلود استفاده می‌شود.

---

## ویژگی‌های فنی و امنیت
- موتور دانلود: ترکیبی از yt-dlp، Instaloader و ماژول‌های اختصاصی برای پلتفرم‌های دیگر
- مدیریت همزمانی: async / task queue برای عملیات دانلود همزمان
- کش: جلوگیری از دانلود مجدد محتوا در بازهٔ زمانی مشخص (پیش‌فرض 5 دقیقه)
- محدودکننده نرخ (Rate Limit): حداکثر 100 درخواست/دقیقه (قابل تنظیم)
- ولیدیشن ورودی‌ها و فیلتر کردن URLهای مخرب
- پشتیبانی از Proxy و تنظیمات شبکه
- رمزنگاری (در صورت نیاز) برای ذخیرهٔ توکن‌ها و تنظیمات حساس

---

## محدودیت‌ها
- حداکثر حجم هر فایل: 2GB
- Cache TTL پیش‌فرض: 5 دقیقه
- Rate Limit پیش‌فرض: 100 درخواست/دقیقه
- برخی محتواها به دلیل محدودیت‌های پلتفرم قابل دانلود نیستند (محتوای خصوصی یا حفاظت‌شده)

---

## لاگ، مانیتورینگ و آمار
- لاگ‌های اپلیکیشن (INFO/ERROR) در فایل یا سرویس لاگینگ ذخیره می‌شود
- Endpoint `/stats` برای جمع‌آوری آمار سرویس
- پیشنهاد: ادغام با Prometheus/Grafana برای مانیتورینگ دقیق

---

## توسعه و مشارکت
- قوانین مشارکت:
  - ابتدا یک issue باز کنید و طرح یا باگ را توصیف کنید
  - قبل از ارسال PR، از فرمت کد و تست‌ها اطمینان حاصل کنید
- پیشنهادات آینده:
  - افزودن پلتفرم‌های بیشتر
  - رابط وب تعاملی برای کاربران نهایی
  - اپ موبایل و سیستم پرداخت

---

## نکات حقوقی و استفاده مسئولانه
کاربران مسئول رعایت قوانین حق نشر و شرایط استفادهٔ پلتفرم‌ها هستند. SHARK تنها ابزار فنی است و توسعه‌دهنده مسئول نقض حقوق سوی کاربر نیست.

---

## تماس و پشتیبانی
- توسعه‌دهنده: K-z89  
  
- مخزن: https://github.com/K-z89/Shark-

---

## مجوز
این پروژه تحت مجوز MIT منتشر شده است. جزئیات در فایل LICENSE.

